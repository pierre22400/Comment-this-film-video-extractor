{
  "name": "comment-this-film",
  "version": "0.2.0",
  "private": true,
  "description": "Cycle 2 — Extension Chrome MV3 : capture de frames d'une vidéo HTML5 puis description visuelle via un relais Gemini local.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p server/tsconfig.json",
    "test": "vitest run",
    "server": "tsx server/index.ts",
    "server:watch": "tsx watch server/index.ts"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "2.7.1",
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.5",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "tsx": "^4.19.2",
    "typescript": "5.7.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
